to run dev

in svelte directory run

```
npm run dev
```

then in root directory run

```
neu run --frontend-lib-dev
```

to build run

in svelte diretory run

```
npm run build
```

then in root directory run

```
neu build
```
